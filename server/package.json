{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd src && node index.js",
    "dev": "cd src && nodemon index.js",
    "dev-admin": "cd ../admin-dashboard && npm run build && cd ../server && nodemon index.js",
    "build": "npm install && cd ../admin-dashboard && npm install && npm run build && cd ../client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google/generative-ai": "^0.22.0",
    "@supabase/supabase-js": "^2.48.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "openai": "^4.85.1",
    "punycode": "^2.3.1",
    "svix": "^1.59.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.9",
    "nodemon": "^3.1.9"
  }
}
